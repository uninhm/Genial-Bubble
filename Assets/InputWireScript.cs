using UnityEngine;

public class InputWireScript : MonoBehaviour
{
    public ButtonActivate button;
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        if (button.I)
    }
}
